import React from 'react';
import PropTypes from 'prop-type';

const AdminMenuView = props => {
    return (
        <Menu theme="dark" aside>
            <MenuList>
                <MenuItem></MenuItem>
            </MenuList>
        </Menu>
    );
};

AdminMenuView.propTypes = {
    
};

export default AdminMenuView;