import React from 'react';
import PropTypes from 'prop-types';

const ServiceHomeView = props => {
    return (
        <div>
            
        </div>
    );
};

ServiceHomeView.propTypes = {
    
};

export default ServiceHomeView;