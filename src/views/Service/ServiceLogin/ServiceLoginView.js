import React from 'react';
import PropTypes from 'prop-types';

const ServiceLoginView = props => {
    return (
        <main className="login-view login-view--service">
            <p>lolwut</p>
        </main>
    );
};

ServiceLoginView.propTypes = {
    
};

export default ServiceLoginView;