import React from 'react';
import PropTypes from 'prop-types';

const MenuItem = props => {
    return (
        <div>
            
        </div>
    );
};

MenuItem.propTypes = {
    
};

export default MenuItem;