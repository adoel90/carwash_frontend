import React from 'react';
import classNames from 'classnames';

const InputAddonRight = props => {
      const {
            className,
            children
      } = props;

      return <div className="input-addon-right">{children}</div>
}

export default InputAddonRight;